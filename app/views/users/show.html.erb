<div class="formcenter">
  <div class="panel panel-info">
    <div class="panel-heading"><h3 class="text-center"><%=@user.name%></h3></div>
    <div class="panel-body">
      <p><strong>Last sign in:</strong> <%=@user.last_sign_in_at.strftime("%H:%M %d.%m.%Y")%></p>
      <p><strong>Email:</strong> <%=@user.email%></p>
    </div>
  </div>
</div>
<br>
<%= render :partial => "orders/orders", :@orders => Order.where(user_id: current_user.id) %>

